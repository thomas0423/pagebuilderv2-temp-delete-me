{
  "name": "pagebuilderv2",
  "version": "1.0.0",
  "private": true,
  "description": "AI-capable pagebuilder CMS — Laravel API + React admin",
  "scripts": {
    "dev": "concurrently -n api,admin -c blue,magenta \"npm run dev:api\" \"npm run dev:admin\"",
    "dev:api": "cd apps/api && php artisan serve --host=127.0.0.1 --port=8000",
    "dev:admin": "cd apps/admin && npm run dev -- --host 127.0.0.1 --port 5173",
    "setup": "cd apps/api && php artisan migrate --force && php artisan db:seed --force && php artisan storage:link && cd ../admin && npm install",
    "build:admin": "cd apps/admin && npm run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
